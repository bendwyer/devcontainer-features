{
  "name": "CloudFormation Linter",
  "id": "cfn-lint",
  "version": "1.0.0",
  "description": "Validate AWS CloudFormation yaml/json templates against the AWS CloudFormation resource provider schemas and additional checks. Includes checking valid values for resource properties and best practices.",
  "documentationURL": "https://github.com/aws-cloudformation/cfn-lint/blob/main/README.md",
  "options": {
    "version": {
      "default": "latest",
      "description": "Select the version to install. Anything that is not 'latest' must be in '#.#.#' format.",
      "proposals": [
        "latest",
        "1.51.0"
      ],
      "type": "string"
    }
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "kddejong.vscode-cfn-lint"
      ]
    }
  },
  "dependsOn": {
    "ghcr.io/devcontainers/features/python": {
      "installTools": "true",
      "toolsToInstall": [
        "virtualenv"
      ]
    }
  }
}