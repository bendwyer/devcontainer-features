{
  "id": "claude-code",
  "version": "4.0.1",
  "name": "Claude Code",
  "documentationURL": "https://docs.anthropic.com/en/docs/claude-code/overview",
  "description": "Claude Code is an agentic coding tool that lives in your terminal, understands your codebase, and helps you code faster by executing routine tasks, explaining complex code, and handling git workflows - all through natural language commands.",
  "options": {
    "version": {
      "default": "stable",
      "description": "Select the version to install.",
      "proposals": [
        "stable",
        "latest",
        "1.0.81"
      ],
      "type": "string"
    }
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "Anthropic.claude-code"
      ]
    }
  },
  "postCreateCommand": "sudo chown $(whoami) /var/lib/claude && mkdir -p /var/lib/claude/ide ~/.claude && rm -rf ~/.claude/ide && ln -sfn /var/lib/claude/ide ~/.claude/ide && claude install $(cat /usr/local/share/claude-version 2>/dev/null || echo stable)",
  "dependsOn": {
    "ghcr.io/devcontainers/features/common-utils": {}
  },
  "containerEnv": {
    "CLAUDE_CONFIG_DIR": "/var/lib/claude"
  },
  "mounts": [
    {
      "source": "${containerWorkspaceFolderBasename}-claude-code",
      "target": "/var/lib/claude",
      "type": "volume"
    }
  ]
}